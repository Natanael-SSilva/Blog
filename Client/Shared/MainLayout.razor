@inherits LayoutComponentBase
@using Microsoft.AspNetCore.Components.Authorization
@using Blog.Client.Services
@using MudBlazor
@inject AuthenticationStateProvider AuthStateProvider
@inject NavigationManager Navigation

<MudLayout>
    <MudAppBar Elevation="1">
        <MudText Typo="Typo.h5" Class="ml-3">Minha Landing Page</MudText>
        <MudSpacer />
        <MudNavMenu Class="nav-menu-center">
            <MudNavLink Href="" Match="NavLinkMatch.All" Icon="@Icons.Material.Filled.Home">Home</MudNavLink>
            <MudNavLink Href="login">Sobre</MudNavLink>
            <MudNavLink Href="contato">Contato</MudNavLink>
        </MudNavMenu>
        <MudSpacer />
        <AuthorizeView>
            <Authorized>
                <MudButton Variant="Variant.Filled" Color="Color.Secondary" OnClick="Logout">Logout</MudButton>
            </Authorized>
            <NotAuthorized>
                <MudButton Variant="Variant.Text" Color="Color.Primary" Href="/login">Login</MudButton>
            </NotAuthorized>
        </AuthorizeView>
    </MudAppBar>
    <MudMainContent>
        <MudContainer MaxWidth="MaxWidth.Large" Class="mt-4">
            @Body
        </MudContainer>
    </MudMainContent>
</MudLayout>

@code {
    private async Task Logout()
    {
        await ((CustomAuthStateProvider)AuthStateProvider).GetAuthenticationStateAsync();
        await new HttpClient().PostAsync($"{Navigation.BaseUri}api/auth/logout", null);
        ((CustomAuthStateProvider)AuthStateProvider).NotifyUserLogout();
        Navigation.NavigateTo("/");
    }
}